[build]
  command = "{{BUILD_COMMAND}}"
  publish = "{{OUTPUT_DIR}}"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "{{DEV_COMMAND}}"
  port = {{DEV_PORT}}
  autoLaunch = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}
