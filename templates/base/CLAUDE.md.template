# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Tech Stack

{{TECH_STACK}}

## Commands

```bash
{{PACKAGE_MANAGER}} run dev          # Dev server
{{PACKAGE_MANAGER}} run build        # Production build
{{PACKAGE_MANAGER}} run test         # Unit tests
{{PACKAGE_MANAGER}} run test:e2e     # E2E tests (Playwright)
{{PACKAGE_MANAGER}} run lint         # Lint code
```

## Project Structure

<!-- Describe your project's file organization -->

## Development Notes

<!-- Add project-specific patterns, gotchas, or important context -->

## Before Committing

1. Run type checker: `{{PACKAGE_MANAGER}} run check`
2. Run linter: `{{PACKAGE_MANAGER}} run lint`
3. Run tests: `{{PACKAGE_MANAGER}} run test`
4. Test manually in browser/client
